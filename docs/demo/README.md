# Deep Research Assistant Demo

Standalone SvelteKit demo frontend for the Deep Research Assistant runtime.

## Commands

```bash
npm install
npm run dev
```

```bash
npm run build
npm run preview
```

## API configuration

The app is static and expects a live backend instance. Set the API base URL in the UI, for example:

```text
http://localhost:8080/v1
```
